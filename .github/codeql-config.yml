---
# CodeQL configuration for CanvasNet

name: "CanvasNet CodeQL Config"

# Exclude build output directories from analysis - these contain generated
# files (e.g. XunitAutoGeneratedEntryPoint.cs) that trigger false-positive
# static analysis findings and are not part of the reviewed source tree.
paths-ignore:
  - "**/obj/**"
  - "**/bin/**"
