namespace CanvasNet.Codecs;

/// <summary>
///     The <see cref="CanvasNet.Codecs"/> namespace provides codecs for loading and saving pixel
///     data in common image file formats: uncompressed Windows BMP (<see cref="BmpCodec"/>), a
///     restricted, dependency-free subset of PNG (<see cref="PngCodec"/>), a restricted,
///     dependency-free subset of TIFF (<see cref="TiffCodec"/>), and a restricted,
///     dependency-free subset of baseline JPEG (<see cref="JpegCodec"/>). Each codec converts
///     to and from a <see cref="CanvasNet.Canvas.Surface"/> pixel buffer using only its existing
///     public API - no format-specific members are added to <see cref="CanvasNet.Canvas.Surface"/>
///     itself, so new codecs can be added independently in the future.
/// </summary>
internal static class NamespaceDoc
{
}
